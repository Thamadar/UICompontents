<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type TextElement}">
        <Setter Property="FontFamily" Value="{DynamicResource DefaultFontFamily}" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource DefaultFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource NormalFontSize}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- default -->
    <Style x:Key="TextBlockDefaultStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Padding" Value="4,7,4,4" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- menu -->
    <Style x:Key="TextBlockMenuStyle" BasedOn="{StaticResource TextBlockDefaultStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Padding" Value="0,7,4,4" />
    </Style>

    <!-- normal -->
    <Style x:Key="TextBlockNormalStyle" BasedOn="{StaticResource TextBlockDefaultStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="Foreground" Value="Black" />
    </Style>

    <!-- medium -->
    <Style x:Key="TextBlockMediumStyle" BasedOn="{StaticResource TextBlockDefaultStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{DynamicResource MediumFontSize}" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="Foreground" Value="Black" />
    </Style>
</ResourceDictionary>